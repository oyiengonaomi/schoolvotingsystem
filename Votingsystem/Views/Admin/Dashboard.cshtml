@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="container mt-4">
    <div class="jumbotron bg-dark text-white p-5 rounded">
        <h1>Admin Control Center</h1>
        <p class="lead">System-wide management and user oversight.</p>
    </div>

    <div class="row mt-4">
        <div class="col-md-4">
            <div class="card border-danger">
                <div class="card-body">
                    <h5 class="card-title">Manage Users</h5>
                    <p>View, edit, or remove registered students.</p>
                    <a href="#" class="btn btn-danger">Go to Users</a>
                </div>
            </div>
        </div>
    </div>
</div>
